export interface MovieDTO{
    title: string,
    description: string,
    duration: number,
    release: number
}

export interface InsertMovieDTO{
    id: string,
    title: string,
    description: string,
    duration: number,
    release:number
}